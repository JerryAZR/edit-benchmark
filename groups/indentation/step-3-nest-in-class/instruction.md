Edit src/layout.py: nest the entire `outer()` function inside a new class called `Wrapper`. Add `class Wrapper:` before `def outer():` and indent `outer` and everything inside it by 4 additional spaces.
