Edit src/layout.py: wrap the body of `outer()` in a try/except block. After the docstring, add `try:` with correct indentation. Indent the existing body by 4 extra spaces. Add `except Exception:` and `return -1` at the end (same indent as try).
